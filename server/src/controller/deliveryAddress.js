import { httpStatus, apiStatus } from '../constants/index.js';
import {DeliveryAddress}  from '../model/deliveryAddress'

const deliverControler = {}

export const insertToDatabase = async (req, res) => {
    try {
        
    }
}